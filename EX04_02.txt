#include <iostream>
#include <ctime>
using namespace std;

int* doubleCapacity(const int* list, int size)
{
	int doublesize = size * 2;
	int* newarray = new int[doublesize];
	
	for (int i = 0, count = 1; i < doublesize; i++; count++)
	{
		newarray[i] = count;
	}
	
	return newarray;
}

void getArray(const int* newarray, int doublesize)
{
	for(int i = 0, i < size, i++)
	{
		cout << newarray[i] << " ";
	}
}
		
int main()
{
	int list[] = {1, 2, 3, 4, 5, 6};
	int* p = doubleCapacity(list, 6);
	getArray(p, 12);
	
	return 0;
}
	
	
